---
const features = [
  {
    feature: "Garantizamos alta calidad.",
    description:
      "Materiales de primera calidad y procesos de construcción sostenibles.",
    icon: "check",
  },
  {
    feature: "Transacción segura",
    description:
      "La transacción se realiza a través de un sistema de pago seguro y confiable.",
    icon: "lock",
  },
  {
    feature: "Costos bajos e impuestos reducidos",
    description:
      "Ofrecemos precios competitivos y asesoría para la reducción de impuestos.",
    icon: "tax",
  },
];
---

<section
  class="w-full flex flex-col md:flex-row items-center gap-y-10 md:gap-x-10 mt-20 md:mt-60 px-0 md:px-0"
>
  <img
    src="/assets/images/feature-img.png"
    alt="some image of a furniture"
    class="w-full md:w-[540px] max-w-full md:max-w-[540px] rounded-none md:rounded-[0_0_120px_0] object-cover m-0"
  />
  <div
    class="flex flex-col md:items-start items-center text-center md:text-left gap-y-4 w-full"
  >
    <h2 class="text-3xl font-bold font-headings">
      Elige entre diferentes tipos de casa
    </h2>
    <p class="w-full md:w-[70%] mx-auto md:mx-0 text-lg text-normal-text">
      Proveemos una amplia selección de tipos de casa para ti y tu familia que
      permiten disfrutar de un espacio único, sin afectar negativamente el medio
      ambiente.
    </p>
    <ul class="mt-8 flex flex-col gap-y-8 w-full">
      {
        features.map((feature) => (
          <li class="flex flex-col md:flex-row items-center md:items-start gap-y-2 md:gap-y-0 gap-x-0 md:gap-x-6 text-center md:text-left w-full">
            <div class="flex-shrink-0 bg-gray-100 rounded-lg w-14 h-14 flex items-center justify-center mx-auto md:mx-0 mb-2 md:mb-0">
              {feature.icon === "check" && (
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  class="w-7 h-7 text-accent-color"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor"
                  stroke-width="2"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    d="M5 13l4 4L19 7"
                  />
                </svg>
              )}
              {feature.icon === "lock" && (
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  class="w-7 h-7 text-accent-color"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor"
                  stroke-width="2"
                >
                  <rect width="16" height="10" x="4" y="7" rx="2" />
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    d="M8 7V5a4 4 0 1 1 8 0v2"
                  />
                </svg>
              )}
              {feature.icon === "tax" && (
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  class="w-7 h-7 text-accent-color"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor"
                  stroke-width="2"
                >
                  <rect x="3" y="7" width="18" height="13" rx="2" />
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    d="M16 3v4M8 3v4"
                  />
                </svg>
              )}
            </div>
            <div class="px-4 md:px-0">
              <h3 class="font-bold text-lg text-main-text">
                {feature.feature}
              </h3>
              <p class="text-normal-text mt-1">{feature.description}</p>
            </div>
          </li>
        ))
      }
    </ul>
  </div>
</section>
